# hokkaido-grid

Data pipeline correlating Hokkaido electricity demand (HEPCO) with
weather observations (Open-Meteo). Fetch -> validate -> transform ->
SQLite -> reported via CLI. Runs unattended on cron.

Status: project setup. First data flowing ~August 2026.
